<h1>Les hopitaux</h1>
<ul>

</ul>
<% @hospitals.each do |hospital| %>
	<li><b><%= hospital.name %></b> : <%= hospital.free_beds_number %> lits disponibles (sur <%= hospital.total_beds_nb %>)</li>
<% end %>
<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>
